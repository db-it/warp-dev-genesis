set -gx HOMEBREW_NO_ANALYTICS 1

# Do not cleanup after install. You can enable this, if you don't want homebrew
# to uninstall old packages after installation or upgrade of packages.
# set -gx HOMEBREW_NO_INSTALL_CLEANUP 1

# Tell homebrew to not autoupdate every single time I run it (just once a week).
set -gx HOMEBREW_AUTO_UPDATE_SECS=604800

abbr --add brewcout --position command brew outdated --cask --greedy
abbr --add brewcupg --position command brew upgrade --cask --greedy
